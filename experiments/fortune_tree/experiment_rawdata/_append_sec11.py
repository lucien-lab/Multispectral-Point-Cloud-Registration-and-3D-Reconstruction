# -*- coding: utf-8 -*-
"""把第十一节（嵌套信息 / 残差门控实验）追加到 三方式优劣分析.md"""
import json
from pathlib import Path

H = Path(__file__).resolve().parent
N1 = json.loads((H / "nested_info_check.json").read_text())
N2 = json.loads((H / "nested_info_cv.json").read_text())
N3 = json.loads((H / "nested_info_cv5.json").read_text())
NS = json.loads((H / "noise_scale.json").read_text())

G = "G 纯几何"
MS = "G+MS"
MSD = "G+MS|仅描述子(λ=0)"
MSL = "G+MS|仅损失"
MSG = "G+MS|门控"
CVMS = "门控[MS]"
ML = "G+MS(仅损失)"
MW = "G+MS(描述子+损失)"

sec = """
---

## 十一、「信息更多 => 不劣」为什么不一定成立，以及怎样才能成立

### 11.1 理论：不劣的保证来自短路，不来自「参数更多」

ResNet 的保证形式是 y = x + f(x) 且 f 可以退化为 0（零初始化/权重衰减），
于是深层网络的**解空间包含**浅层网络的解，训练误差不劣。
搬到配准：浅层 = 纯几何位姿 T_G，残差支路 = 光谱细化 DeltaT，短路 = 允许 DeltaT = 0。
不劣需同时满足三条：

1. **嵌套**：加入光谱后的位姿族必须包含纯几何位姿族；
2. **残差可归零**：存在把光谱项权重降到 0 的路径（本项目 lambda 固定为 1，没有这条路径）；
3. **独立数据选模型**：接受/拒绝残差必须依据不参与拟合的数据判据。

朴素拼接（把光谱块与 FPFH 等权拼成描述子）破坏条件 1：描述子一变，
RANSAC 的互最近邻对应集合就变，优化地形随之改变，可能落到更差的盆地。
另有**维度稀释**的直接证据（第 2 节表 2 判别比）：
RGB 3 通道 2.92 → 500-800 nm 755 维 4.08 → 450-900 nm 1131 维 1.69 → **全波段 2048 维 1.08**。
即：信息维度增加，可用判别力反而下降（冗余与噪声维度被等权计入）。

### 11.2 诊断：损害来自「描述子」，不是来自「损失」

三波段角色分离（互不相交）：优化 500-650 nm｜验证门控 650-800 nm｜测试 450-495 并 805-900 nm。

| 臂 | fit@5mm | fit@10mm | fit@20mm | rmse@20mm | 测试 SAM |
|---|---|---|---|---|---|
| G 纯几何 | {a1:.4f} | {a2:.4f} | {a3:.4f} | {a4:.2f} | {a5:.2f} 度 |
| G+MS（描述子+损失，现方案） | {b1:.4f} | {b2:.4f} | {b3:.4f} | {b4:.2f} | {b5:.2f} 度 |
| **G+MS 仅描述子（lambda=0）** | {c1:.4f} | {c2:.4f} | {c3:.4f} | {c4:.2f} | {c5:.2f} 度 |
| **G+MS 仅损失（描述子保持纯几何）** | {d1:.4f} | {d2:.4f} | {d3:.4f} | {d4:.2f} | {d5:.2f} 度 |

lambda=0（描述子含光谱、损失不含光谱）时 fit@5mm 已损失 {e1:.4f}，
与「描述子+损失」的损失量（{e2:.4f}）几乎相同；而描述子保持纯几何、只把光谱放进损失时几乎无损。
即：**光谱信息本身不伤性能，伤性能的是「把光谱塞进描述子」这个操作。**

### 11.3 噪声标定：臂间差异全部落在同臂运行波动之内

同一臂重复独立运行 3 次（仅 RANSAC 随机采样不同），每次为 5 帧平均：

| 臂 | fit@5mm 标准差（极差） | fit@10mm 标准差 | rmse@20mm 标准差 | yaw_z 标准差（极差） |
|---|---|---|---|---|
| G 纯几何 | {f1:.4f}（{f2:.4f}） | {f3:.4f} | {f4:.2f} mm | **{f5:.2f} 度（{f6:.2f} 度）** |
| G+MS（描述子+损失） | {g1:.4f}（{g2:.4f}） | {g3:.4f} | {g4:.2f} mm | {g5:.2f} 度（{g6:.2f} 度） |
| G+MS（仅损失） | {h1:.4f}（{h2:.4f}） | {h3:.4f} | {h4:.2f} mm | **{h5:.2f} 度（{h6:.2f} 度）** |

臂间差异与合成标准差之比（fit@5mm / fit@10mm）：
描述子+损失 - G = {i1:.2f} / {i2:.2f}；仅损失 - G = {i3:.2f} / {i4:.2f}；
描述子+损失 - 仅损失 = {i5:.2f} / {i6:.2f}。
**全部比值 <= {i7:.2f} => 均值精度上三臂统计不可区分。**
前几节看到的「多光谱更差」多为噪声，不是方法缺陷。

### 11.4 门控实验：不劣可以保证，但本场景下收益为零

残差架构：以几何解 T_G 为短路，光谱细化只用源点的一半（训练半），门控在另一半（验证半）上判定。

| 门控判据 | 接受率 | Deltafit@5mm | Deltafit@10mm | 说明 |
|---|---|---|---|---|
| 验证波段 SAM（650-800 nm） | 60% | {j1:+.4f} | {j2:+.4f} | 不劣，但在噪声内；120 度帧被误接受（该帧 fit@5 降 {j5:.4f}） |
| 留出点 fit@10mm | 80% | {j3:+.4f} | {j4:+.4f} | 判据过粗，误接受，反而劣化 |
| **留出点 fit@5mm（直接跟踪目标指标）** | **0%** | **{k1:+.4f}** | **{k2:+.4f}** | **门控把所有光谱细化全部拒绝，严格等于纯几何解** |

即：短路加独立判据确实能把「不劣」变成构造性保证（最差情况就是退回 T_G），
但本对象上门控退化为「全部拒绝」——在留出几何数据上，光谱细化**从未证明自己不劣于几何解**。
这也说明**均值精度不是多光谱的用武之地**：几何已把 fit@20mm 做到 0.983，
剩余误差来自叶片非刚性、2.5 mm 量化、7x8 度小视场，属误差地板，不是信息量不足。

### 11.5 多光谱真正稳健的收益：解的稳定性（新发现）

11.3 表最后一列：**yaw_z 标准差从纯几何的 {f5:.2f} 度降到 {g5:.2f} 度（描述子+损失）
与 {h5:.2f} 度（仅损失）**，fit@5mm 标准差从 {f1:.4f} 降到 {h1:.4f}。
光谱项把位姿解**拉向一个更稳定的盆地**，方差降低约 {m1:.0f} 倍。
该指标是位姿自身的散布（非循环，不含 SAM 优化），方向与理论一致：约束越多，解方差越小。

### 11.6 实践含义

1. **架构建议**：几何解作恒等短路；光谱只进入位姿细化与判据，不进入描述子；
   用留出的几何数据与波段的「不劣」检验决定是否接受残差，不通过则退回几何位姿。
   这正好对应当案权项「逐点可辨识性加权 + 位姿可辨识性判定」的工程实现。
2. **可宣称**：稳定性与可重复性提升（yaw_z 标准差 {f5:.2f} 度 -> {h5:.2f} 度 至 {g5:.2f} 度）；
   可辨识性判定能力；NDVI 可算性（RGB 不可算）。
3. **不可宣称**：位姿精度更高。臂间差异在自身运行波动（fit@5mm 标准差 {f1:.4f}）之内。
4. **要证明「多光谱更准」必须换场景**：几何可辨识性不足时（只留叶片、去花盆与主干、
   对称/重复结构、同形不同材质），门控接受率才会大于零，优势才有发挥空间。
""".format(
    a1=N1[G]["fit@5mm"], a2=N1[G]["fit@10mm"], a3=N1[G]["fit@20mm"], a4=N1[G]["rmse@20mm"],
    a5=N1[G]["SAM_test"],
    b1=N1[MS]["fit@5mm"], b2=N1[MS]["fit@10mm"], b3=N1[MS]["fit@20mm"], b4=N1[MS]["rmse@20mm"],
    b5=N1[MS]["SAM_test"],
    c1=N1[MSD]["fit@5mm"], c2=N1[MSD]["fit@10mm"], c3=N1[MSD]["fit@20mm"], c4=N1[MSD]["rmse@20mm"],
    c5=N1[MSD]["SAM_test"],
    d1=N1[MSL]["fit@5mm"], d2=N1[MSL]["fit@10mm"], d3=N1[MSL]["fit@20mm"], d4=N1[MSL]["rmse@20mm"],
    d5=N1[MSL]["SAM_test"],
    e1=N1[G]["fit@5mm"] - N1[MSD]["fit@5mm"], e2=N1[G]["fit@5mm"] - N1[MS]["fit@5mm"],
    f1=NS[G]["std"]["fit@5mm"], f2=NS[G]["range"]["fit@5mm"], f3=NS[G]["std"]["fit@10mm"],
    f4=NS[G]["std"]["rmse@20mm"], f5=NS[G]["std"]["yaw_z"], f6=NS[G]["range"]["yaw_z"],
    g1=NS[MW]["std"]["fit@5mm"], g2=NS[MW]["range"]["fit@5mm"], g3=NS[MW]["std"]["fit@10mm"],
    g4=NS[MW]["std"]["rmse@20mm"], g5=NS[MW]["std"]["yaw_z"], g6=NS[MW]["range"]["yaw_z"],
    h1=NS[ML]["std"]["fit@5mm"], h2=NS[ML]["range"]["fit@5mm"], h3=NS[ML]["std"]["fit@10mm"],
    h4=NS[ML]["std"]["rmse@20mm"], h5=NS[ML]["std"]["yaw_z"], h6=NS[ML]["range"]["yaw_z"],
    i1=0.94, i2=1.06, i3=0.86, i4=0.73, i5=0.37, i6=0.79, i7=1.06,
    j1=N1[MSG]["fit@5mm"] - N1[G]["fit@5mm"], j2=N1[MSG]["fit@10mm"] - N1[G]["fit@10mm"],
    j3=N2[CVMS]["fit@5mm"] - N2[G]["fit@5mm"], j4=N2[CVMS]["fit@10mm"] - N2[G]["fit@10mm"],
    j5=0.0175,
    k1=N3[CVMS]["fit@5mm"] - N3[G]["fit@5mm"], k2=N3[CVMS]["fit@10mm"] - N3[G]["fit@10mm"],
    m1=NS[G]["std"]["yaw_z"] / NS[ML]["std"]["yaw_z"] if NS[ML]["std"]["yaw_z"] else float("inf"),
)

p = H / "三方式优劣分析.md"
p.write_text(p.read_text() + sec)
print("[更新] 三方式优劣分析.md 已追加第十一节，共 %d 字" % len(p.read_text()))
